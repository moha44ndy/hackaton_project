services:
  wmdp-test:
    build:
      dockerfile: Dockerfile
    env_file:
      - ./exemple.env
    volumes:
      - ./src:/app/src
      - ./data:/app/data
      - ./results:/app/results
      - ./logs:/app/logs
      - ./runner:/app/runner
    